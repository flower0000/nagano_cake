<div class="container">
  <div class="row">
    <div class="col-lg-1 bg-secondary">
      商品詳細
    </div>
  </div>
  <div class="row">

    <div class="col-lg-3">
      <%= attachment_image_tag @item, :image, :fill, 100, 100, format: 'jpeg', fallback: "no_image.jpg", size:'100x100' %>
    </div>

    <div class="col-lg-5">
      <div>商品名　<%= @item.name %></div>
      <div>商品説明　<%= @item.introduction %></div>
      <div>税込み価格(税抜き価格)　<%= @item.price * 1.10 %>(<%= @item.price %>)円</div>
    </div>
  </div>
<!--もし揃えたいならtableで1要素ずつで並べる-->
<!--項目は左端に縦積み-->

  <div class="row">
    <div class="mx-auto">
      <%= link_to "編集する", edit_admin_item_path(@item.id), class:"btn btn-success" %>
    </div>
  </div>

</div>